# Fresh source-repository record

The source-only publication repository is:

`https://github.com/turbineBMW/surface-pro-11-linux`

It was created as a new GitHub repository from a local `git archive` of the
reviewed source tree, not by changing the visibility of the former repository.
Its unrelated root identity is:

```text
commit 05e5335bc72476d44390336701cf03efa5fd0165
tree   13e61d8864076955a387eae8dbeacceac1c1804e
```

Before publication, the fresh repository reported one `main` branch, no tags,
releases, forks, pull requests, issues, or Actions runs. Its complete object
graph, embedded kernel and libcamera bundles, cumulative patches, and generated
`git archive` passed the reconstruction, path, provenance-marker, credential,
and license checks recorded in `VALIDATION.md`.

The former repository was renamed
`turbineBMW/surface-pro-11-linux-legacy-private` and remains private. The new
repository does not descend from or share the former Git history.

This record authorizes only the experimental source repository. Prebuilt
kernels, modules, userspace binaries, payloads, firmware, disk images, and ISOs
remain blocked by `BINARY-RELEASE-HOLD.md`.
